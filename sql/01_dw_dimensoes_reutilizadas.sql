PROMPT === Recriando DIM_TEMPO e DIM_PRODUTO ===

BEGIN
    EXECUTE IMMEDIATE 'DROP TABLE FATO_ESTOQUE CASCADE CONSTRAINTS';
EXCEPTION
    WHEN OTHERS THEN
        IF SQLCODE != -942 THEN RAISE; END IF;
END;
/

BEGIN
    EXECUTE IMMEDIATE 'DROP TABLE DIM_TEMPO CASCADE CONSTRAINTS';
EXCEPTION
    WHEN OTHERS THEN
        IF SQLCODE != -942 THEN RAISE; END IF;
END;
/

BEGIN
    EXECUTE IMMEDIATE 'DROP TABLE DIM_PRODUTO CASCADE CONSTRAINTS';
EXCEPTION
    WHEN OTHERS THEN
        IF SQLCODE != -942 THEN RAISE; END IF;
END;
/

CREATE TABLE DIM_TEMPO (
    SK_TEMPO         NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    DAT_COMPLETA     DATE NOT NULL,
    NUM_ANO          NUMBER(4) NOT NULL,
    NUM_MES          NUMBER(2) NOT NULL,
    NOM_MES          VARCHAR2(20) NOT NULL,
    NUM_TRIMESTRE    NUMBER(1) NOT NULL,
    NUM_DIA_SEMANA   NUMBER(1) NOT NULL,
    NOM_DIA_SEMANA   VARCHAR2(20) NOT NULL,
    CONSTRAINT UK_DIM_TEMPO UNIQUE (DAT_COMPLETA)
);

CREATE TABLE DIM_PRODUTO (
    SK_PRODUTO       NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    COD_PRODUTO      NUMBER(10) NOT NULL,
    NOM_PRODUTO      VARCHAR2(20),
    COD_BARRA        VARCHAR2(20),
    STA_ATIVO        VARCHAR2(20),
    DAT_CADASTRO     DATE,
    DAT_CANCELAMENTO DATE,
    CONSTRAINT UK_DIM_PRODUTO UNIQUE (COD_PRODUTO)
);
